name: Refine review async lifecycle

on:
  push:
    branches:
      - feature/physical-fire-settlement

jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/physical-fire-settlement
      - name: Apply patch
        run: python tools/refine_review_async.py
      - name: Commit production change and clean helpers
        run: |
          git config user.name "HisenWeb"
          git config user.email "actions@users.noreply.github.com"
          git rm .github/workflows/refine-review-async.yml tools/refine_review_async.py
          git add IronNestFCS.Logic/Execution/FirePlanExecutor.cs IronNestFCS.Logic/FCS/TriggerConsole.cs IronNestFCS.Logic/Infrastructure/SharedConsoleCoordinator.cs
          git commit -m "fix: decouple async review buttons from firing state"
          git push origin HEAD:feature/physical-fire-settlement
